const search=document.getElementById('searchBox');const cards=[...document.querySelectorAll('.card')];const count=document.getElementById('paperCount');const empty=document.getElementById('empty');function filterPapers(){const q=search.value.trim().toLowerCase();let n=0;cards.forEach(c=>{const ok=c.dataset.title.toLowerCase().includes(q);c.hidden=!ok;if(ok)n++});count.textContent=`${n} paper${n===1?'':'s'}`;empty.style.display=n?'none':'block'}search.addEventListener('input',filterPapers);filterPapers();